function divisaoResto ( num1, num2 ) {
    resultado = num1 / num2
    resto = num1 % num2
    return "O resultado é " + resultado + " e o resto da divisão é " + resto
}

console.log( divisaoResto (8, 3))